{
  "name": "holo-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-release.mjs",
    "verify:city": "node scripts/verify-city-clearance.mjs",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "vite": "^5.3.0"
  }
}